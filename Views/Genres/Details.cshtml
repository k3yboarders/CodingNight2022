@model LibraryCodingNight.Models.Genre

@{
    ViewData["Title"] = "Szczegóły";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Szczegóły</h1>

<div>
    <h4>Gatynek</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.GenreName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.GenreName)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.GenreId">Edytuj</a> |
    <a asp-action="Index">Powrót do listy</a>
</div>
