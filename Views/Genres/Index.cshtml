@model IEnumerable<LibraryCodingNight.Models.Genre>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Stwórz nowy</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GenreName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GenreName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GenreId">Edytuj</a> |
                <a asp-action="Details" asp-route-id="@item.GenreId">Szczegóły</a> |
                <a asp-action="Delete" asp-route-id="@item.GenreId">Usuń</a>
            </td>
        </tr>
}
    </tbody>
</table>
